%h1 Reports
%table
  %thead
    %tr
      %th{"width" => "150"} Suspect
      %th Reason
      %th Description
      %th{"width" => "150"} Reporter
      %th{"width" => "150"} Reported at
  %tbody
    %tr
      - @reports.each do |report|
        %td= image_tag report.user.profile_picture, class: "icon-size"
        %td= report.reason
        %td= report.description
        %td= image_tag report.reporter.profile_picture, class: "icon-size"
        %td= report.created_at.strftime("%B %d, %Y")
