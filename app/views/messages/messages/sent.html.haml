%h1 SENT
- if ! @messages.blank?
  - @messages.each do |message|
    %ul
      %li= "Title: #{message.title}"
      %li= "Message: #{message.body}"
      %li= "Message to: #{message.receiver}"
      %li
        = link_to "delete", user_messages_delete_sent_path(current_user.id, message.id),
                            method: :delete,
                            data: {confirm: "This will permanenly delete it, are you sure?"}
- else
  %h3 no messages
