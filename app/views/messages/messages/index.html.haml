%h1 INBOX
- if ! @messages.blank?
  - @messages.each do |message|
    %ul
      %li= "Title: #{message.title}"
      %li= "Message: #{message.body}"
      %li= "Message from: #{message.sender}"
      = link_to "reply", new_user_message_path(message.posted_by)
- else
  %p No messages
