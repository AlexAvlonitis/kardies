.row
  .small-6.small-centered.columns
    - flash.each do |name, msg|
      - if msg.is_a?(String)
        %div{ "data-alert" => "", "class" => "alert-box #{foundation_class_for(name)}" }
          = msg
          %a.close.pull-right{"href" => "#"} ⊗
