%h2.display-5= t '.galleries'
%hr
- if @user.gallery.blank?
  .slick-images
    - 6.times do
      = holder_tag '300', "#{t '.empty'}", nil, {}, { bg: '#E8117F' }
- else
  .slick-images
    - @user.gallery.pictures.each do |pic|
      %img.gallery-dimensions{ 'data-lazy' => "#{pic.picture.url}" }
