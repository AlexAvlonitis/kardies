.pull-right
  %p περιοχή
  = form_tag users_path, method: :get do
    = select_tag "q", options_for_select(GC.cities('att'))
    = submit_tag 'Search'
.col-md-8.col-md-offset-2
  %h1 All users

  - @users.each do |user|
    - present user do |user_presenter|
      .col-sm-5.col-md-3
        .thumbnail
          = user_presenter.profile_picture_link(:medium)
          %span.pull-right
            = "Online now" if user.is_signed_in
          .caption
            %span= user.full_name
            %span.pull-right Age: #{user.age}
            %p
              = user_presenter.new_message
              = user_presenter.like_status
