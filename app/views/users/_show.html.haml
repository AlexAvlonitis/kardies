%h1 All users
- @users.each do |user|
  %ul
  %li= "#{user.first_name} #{user.last_name} "
  %p Is going to visit:
  - user.future_places.each do |place|
    %ul
      %li
        =render_country(place.country)
        =render_state(place.state)
        =render_city(place.city)
        on: #{place.visit_date}
