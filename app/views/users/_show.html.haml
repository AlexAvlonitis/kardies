%h1 All users
- @users.each do |user|
  %ul
  %li= "#{user.first_name} #{user.last_name} "
  %p future places to visit
  - user.future_places.each do |places|
    %ul
    ="#{places.country} / #{places.city} on: #{places.visit_date}"
